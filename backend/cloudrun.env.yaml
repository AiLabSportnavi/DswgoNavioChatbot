# Non-secret runtime config for Cloud Run (passed via --env-vars-file).
# Secrets (Azure API key, Supabase service-role key) are NOT here — they live in
# Secret Manager and are wired in with --set-secrets. All values must be strings.
AZURE_AI_CHATBOT_OPENAI_ENDPOINT: "https://ai-chatbot-projekt-resource.openai.azure.com/openai/v1"
AZURE_AI_CHATBOT_DEPLOYMENT_NAME: "gpt-4.1"
SUPABASE_URL: "https://rbhodgwareakqlqcybrl.supabase.co"
ALLOWED_ORIGINS: "https://sportnavi.de,https://www.sportnavi.de"
RATE_LIMIT_PER_MIN: "15"
RATE_LIMIT_PER_DAY: "300"
MAX_MESSAGE_CHARS: "2000"
MAX_HISTORY_TURNS: "10"
MAX_TOKENS: "800"
TEMPERATURE: "0.4"
SESSION_TTL_MIN: "30"
CONFIG_TTL_SEC: "30"
# Admin auth via Clerk. CLERK_SECRET_KEY is NOT here — set it via --set-secrets.
CLERK_ISSUER: "https://normal-joey-58.clerk.accounts.dev"
ADMIN_EMAIL_DOMAINS: "sportnavi.de,ncr4ailab.de"
